class Rook

  def move_dirs
    #what dir -x, +x, -y, +y
    offsets = [[0,1], [1,0], [-1,0] [0,-1] #need all offsets]
    possible_moves = []
    #3,3]
    poss_moves = [[3,4],[4,3],[2,3],[3,2][]
    
    offsets.each do |offset|
      # unless self.pos[0] + offset[0] > 7 && < 0
        
    end

  end
end